import React from "react";
import ReactDOM from "react-dom";

function CalculatorInput() {
  return (
    <input
      type="text"
      placeholder="Enter an equation or use the regular calc"
      id="calculatorInput"
    />
  );
}
export default CalculatorInput;
